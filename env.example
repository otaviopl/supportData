# Backend Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Database
# For local run, default is ./app.db; in Docker, compose sets /app/data/app.db
DB_PATH=./app.db

# Frontend Configuration (if needed)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Integrations
# Webhook called when a ticket is created or updated to closed/high
N8N_WEBHOOK_URL=https://automation.4devtrance.duckdns.org/webhook/endpoint-test


